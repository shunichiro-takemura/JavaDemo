spring:
  application:
    name: demo

  datasource:
    url: jdbc:postgresql://localhost:5432/demo
    username: demo_user
    password: password

logging:
  level:
    org.springframework.jdbc.datasource: DEBUG
